// Define status common for toast
export enum ToastStatus {
  Success = 'success',
  Error = 'error',
}

// Define status common for leave form
export enum StatusLeave {
  Pending = 'Pending',
  Approved = 'Approved',
  Rejected = 'Rejected',
}
